tesseract:
    data directory: default
    recognition language: eng
    ocr engine mode: 3

input image processing:
    scale factor: 1
    invert: no
    grayscale: no
    blackwhite: no
    blackwhite threshold: 200

